{
  "config": {
    "step": {
      "pick_implementation": {
        "title": "Pick authentication method"
      },
      "reauth_confirm": {
        "title": "Re-authenticate Navimow",
        "description": "The Segway login has expired or been revoked. Please sign in again."
      }
    },
    "abort": {
      "already_configured": "Device is already configured",
      "missing_config": "OAuth2 configuration is missing",
      "reauth_successful": "Re-authentication successful",
      "oauth_error": "Error retrieving the access token",
      "oauth_timeout": "Timed out during authentication",
      "oauth_unauthorized": "OAuth2 authorization failed"
    },
    "create_entry": {
      "default": "Successfully connected to Navimow"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Navimow options",
        "data": {
          "poll_interval_seconds": "REST poll interval (seconds)"
        }
      }
    }
  },
  "entity": {
    "sensor": {
      "battery": {
        "name": "Battery"
      }
    },
    "binary_sensor": {
      "mqtt_connected": {
        "name": "MQTT connected"
      }
    }
  }
}
